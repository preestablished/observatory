[package]
name = "obs-types"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
determinism-proto = { workspace = true, features = ["observatory"] }
thiserror = { workspace = true }
